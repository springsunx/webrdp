# WebRDP 输入与统一入口说明

## 普通连接

不带参数访问时显示连接界面：

```text
http://localhost:3000/
```

填写主机地址、端口、用户名、密码和分辨率后点击“连接”。浏览器只会在 localStorage 中保存 `host`、`port`、`user`、`width`、`height`，不会保存密码。

## 统一协作入口

可使用一个包含完整连接凭据的 URL Fragment：

```text
http://localhost:3000/#host=YOUR_RDP_HOST&port=3389&user=YOUR_USER&password=YOUR_PASSWORD&width=1920&height=1080
```

打开后会自动连接：

- 第一个用户成为主用户并拥有控制权。
- 后续打开相同链接的用户自动进入同一画面，默认观看。
- 观看者点击“接管操作”获得临时控制权，主用户可随时点击“抢回控制权”。
- 临时操作者点击“结束操作并归还”或关闭页面，权限自动回到主用户。
- 主用户断开会结束整个协作会话。

参数值包含 `@`、`#`、`&`、空格或中文时必须进行 URL 编码。应用界面不会显示包含凭据的入口，请由调用系统或可信配置生成并保存原始链接。

## 参数说明

| 参数 | 必填 | 默认值 | 描述 |
|------|------|--------|------|
| `host` | 是 | - | RDP 主机 IP 或域名 |
| `port` | 否 | 3389 | RDP 端口 |
| `user` | 是 | - | Windows 用户名 |
| `password` | 自动连接时是 | - | Windows 密码，仅建议放在 `#` 后 |
| `width` | 否 | 自动计算 | 桌面宽度 |
| `height` | 否 | 自动计算 | 桌面高度 |
| `title` | 否 | WebRDP | 页面标题 |

## 权限界面

- “主用户 · 当前可操作”：首位用户拥有控制权。
- “主用户正在操作 · 当前为观看模式”：参与者只能观看，可点击接管。
- “临时控制中”：当前参与者可以操作，结束或关闭后自动归还。
- “临时用户正在操作”：主用户暂时只读，可点击“抢回控制权”立即恢复操作。

同一时间只有一个控制者。前端不发送非控制者输入，后端还会再次拦截越权的 Guacamole 指令。

## 移动端操作

- 手机端使用 Touchpad 模式：单指移动鼠标，轻触执行点击。
- 双指拖动用于滚动查看超出屏幕的远程桌面。
- 平板、触摸屏或浏览器桌面模式使用 Touchscreen 直接点击模式。
- 没有控制权时仍可滚动查看，但键鼠指令不会发送到远程桌面。

## 安全注意事项

1. 统一入口包含 Windows 密码，只能发送给受信任人员。
2. 必须使用 HTTPS，避免凭据在传输过程中泄露。
3. Fragment 不会发送到 Web 服务器；页面读取后会立即从地址栏删除。
4. 不要使用 `?password=...` 查询参数形式。
5. 生产环境应在 WebRDP 前增加登录认证、访问审计和会话授权。
